func f(p1: integer, p2: integer, p3: integer) -> int
{
    let retval: integer;
    retval = p1 + p2 * p3;
    return (retval);
}

func temp(p1: integer, p3: integer) -> int
{
    let tempa: integer;
    let c: integer;
    c = 4 * 2;
    tempa = p3 - c;
    return (tempa);
}

func main() -> void
{
    let a: integer;
    let b: integer;
    let c: integer;
    a = 1;
    b = 2;
    a = f(a,b,3) * temp(a,3);
    write(- a);
}
